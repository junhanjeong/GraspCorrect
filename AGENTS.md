# 프로젝트 설명
이 프로젝트의 목표는 `paper/` 폴더에 존재하는 논문을 기반으로 논문 재현(reproduction)을 수행하는 것입니다.

`paper/` 폴더에는 다음 파일들이 존재합니다:
- 논문 원본 PDF (`GraspCorrect.pdf`)
- 논문 원본 Markdown (`GraspCorrect.md`)
- figure 이미지 (`*.png`)
- table 이미지 (`*.png`)

반드시 위 파일들을 먼저 읽고 논문을 충분히 이해한 뒤 구현을 진행해야 합니다.

# 코드 구현 원칙
논문을 최대한 재현해서 Python 코드를 작성합니다.

논문에서 사용하는 벤치마크 시뮬레이션(RLBench, CALVIN)에 대해 동작하게 코드를 구현해야하며, 논문에서 사용하는 베이스라인 모델들(PerAct, Act3D, 3D Diff, RVT-2)을 가져와서 사용해서 코드를 구현합니다.

벤치마크 2개에 대해서는 모두 다 구현을 해야하며, 베이스라인 모델은 구현하기 용이한 것을 가져와서 구현하고 모두 구현할 필요는 없습니다. 우선사항이 논문 전체 Pipeline이 제대로 재현되고 동작하는 것을 확인하는 것이기 때문입니다.

논문을 재현할 수 있게 코드를 모두 작성하고, 환경 설치법부터 실행방법까지 작성해주세요.

# 주의사항
** 구현을 하다가 논문 내용만으로는 애매한 부분이 있다면, 사용자에게 반드시 물어보세요. 마음대로 코드를 작성하는 것은 금지합니다. 최대한 사용자와 상호작용을 많이 하세요. **